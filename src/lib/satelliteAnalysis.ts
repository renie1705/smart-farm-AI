// Satellite analysis feature removed.
// This file is kept as a stub to prevent import errors during cleanup.

export type GeoLocation = { lat: number; lng: number };
export type FieldBoundary = { id: string; name: string; area: number; coordinates: GeoLocation[]; createdAt: Date };
export type FieldMetrics = any;
export type Alert = any;
